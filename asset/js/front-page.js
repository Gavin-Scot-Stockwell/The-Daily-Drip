//code+gaming = coolmathgames.com
const container = document.querySelector('#container');

const add = document.querySelector('#add');

document.getElementById('add').addEventListener('click', function() {

const waterIntake = getElementById('waterIntake');//add the stuff 
//making clear by adding NOTHING (hope it works)

const newDay = document.getElementById('container');
const newElement = newDay.cloneNode(true);//if false we would have to tell what child where need to go
document.getElementById('clone').appendChild(newElement);});